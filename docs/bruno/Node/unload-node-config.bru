meta {
  name: Unload Node Config
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/node/config
  body: none
  auth: inherit
}

docs {
  Unloads this node's own config (and every guest node). Use
  `DELETE /node/guests/{guestName}/config` to unload a single guest node.
}
